[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
addopts =
    -q
    -p no:cacheprovider
    --cov=core
    --cov=cli
    --cov=utils
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=85
filterwarnings =
    ignore::DeprecationWarning
